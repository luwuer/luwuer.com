const config = {

  port: 3000,

  root: __dirname,

  database: {
    DATABASE: 'test',
    USERNAME: 'root',
    PASSWORD: '123456',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config
